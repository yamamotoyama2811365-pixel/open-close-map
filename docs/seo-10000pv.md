# 月間10,000PVに向けた改善記録

更新: 2026-09-16 / 対象: 開店閉店マップ

## 目標と未計測事項

目標はサイト全体で月間10,000PV。30日換算では約333PV/日。これは目標であり、現在の実績・達成予測ではない。検索クリック数、訪問数、PVは別の指標として扱う。

GA4プロパティ553356360の一覧上の存在は確認できたが、詳細取得と2026-08-11〜2026-09-09のレポート取得がともに「authorization has expired or been revoked」で失敗した。現在PV、自然検索流入、回遊率は未確認。測定IDは既存のG-ZB4T13GM5Pを保持。サイト上のタグ修正と、分析ツール側の再認証は別の作業。

## 2026-09-10 第1弾

- トップの地域6件・業種6件を最初のHTMLから存在する通常のリンクに変更。
- 動的な店舗カード、直近情報、業種一覧、全国動向の業種を通常のリンクに変更。Ctrl/Cmdクリックやキーボード操作を妨げない。
- クリック計測は既存のdata-ga-eventの仕組みに統一し、独自の画面遷移コードを除去。
- サイト名のWebSite構造化データ、OGP、明示的なrobots指定を追加。架空の評価、順位、店舗情報は追加しない。
- 元のレイアウト・画像・広告・訂正窓口と共用APIを維持。主画像に読み込み優先度を指定したが、速度改善は未実測。
- 計測設定APIのHTTPエラー、JSONエラー、無効なID、ネットワーク障害、3秒タイムアウトで既存IDにフォールバック。同じスクリプトを再読み込みしても初期化を重複させない。
- データ抽出・掲載判定・データベース・料金プランは変更しない。

静的HTML中の重複しない内部リンク先（ルートから始まりフラグメントを含まないhref）は6から18に増加。動的カードはこの数に含めない。この指標はクロールしやすさの確認用であり、検索登録数や検索順位ではない。

## 確認

`node --check app.js` / `node --check analytics.js`

`node scripts/test-seo-growth.mjs` : 13項目成功（設定取得、各種失敗・タイムアウト、遅延応答、重複初期化防止、クリック計測、静的リンク、カード出力・エスケープ）。

オフラインのテストデータを使ったChromium表示確認: 幅390pxと1440pxでJavaScript例外なし、横方向のはみ出しなし。これは本番データ・本番でのGA受信確認ではない。公開反映・Googleの再クロール・検索評価の変化は別途確認する。

## 検索需要の参考値と次の判断

2026-09-10にHYPDのresearch_get_search_volumeで日本・日本語を指定。取得された月間検索数の推定値: 「開店閉店」18,100、「札幌 閉店」1,900、「札幌 開店」480、「福岡 閉店」720、「大阪 閉店」480、「ラーメン 新店」1,300。付属の月別データは2025年8月〜2026年7月。自サイトのPV予測ではなく、類似語間の重複を考慮せず合算しない。広告のcompetition指標をSEO難易度として使わない。

全国の大きな語だけを追わず、「店舗名＋開店/閉店」と「地域＋閉店」を改善候補とする。重点地域は、既存データの件数・鮮度とSearch Consoleの実際の表示・順位を見て決める。確認前に全国一律の薄いページを増やさない。

再認証後の順序:
1. GA4で対象ホストの30日PV、自然検索訪問、詳細ページへの回遊を計測する。
2. Search Consoleでサイトマップ処理、検索登録状況、表示回数・クリック・検索語を確認する。提出済み・登録済みとは未確認のまま断定しない。
3. 表示はあるがクリックされないページは、実データに合ったタイトル・要約を検証する。登録されないページは先に内容の重複・情報不足・正規URL・応答を点検する。
4. 読まれた地域・店舗を軸に、掲載元、日程、所在地、関連店舗への導線を改善する。不明な閉店理由や後継テナントを推測で埋めない。
5. 変更日ごとの7日/28日比較を蓄積する。検索需要の季節変化や記事追加件数の違いも区別し、PVを不自然な再読み込み等で水増ししない。

自動で継続監視・記事修正するジョブはこの変更には含まれていない。

## 2026-09-16 Bing基準値と検索意図対応

Bing Webmaster Toolsで `https://open-close-map.onrender.com/` を実測確認。サイトマップ `https://open-close-map.onrender.com/sitemap.xml` はSuccessで、確認時点では3,880 URLを認識していた。一方、Bingの検索クエリ、ページ、トラフィック統計はクリック0・表示0で、既存の勝ち検索語はまだ存在しなかった。したがって、この段階ではCTR改善ではなく、検索意図への一致とクロール対象の質を優先する。

Bingキーワード統計の日本市場参考値では、「開店閉店」「閉店」「閉店情報」に継続的な検索表示需要があり、「オープン予定」単独よりも強かった。Google系のキーワード需要推定でも「開店閉店」18,100、「閉店」18,100、「札幌 閉店」1,900、「閉店情報」1,600などが確認されている。BingとGoogleの数値は別データであり、混同・合算しない。

共用Open Close API側で以下を本番反映した。

- 地域ページのtitle・H1を「開店閉店情報」「閉店情報」「開店情報」の実検索語へ寄せ、閉店フィルタでは「閉店予定・閉店店舗一覧」、開店フィルタでは「新規オープン・開店予定一覧」を明示。
- `札幌市` のような市ページが `札幌市中央区` 等の区データも集約するようにし、「札幌 閉店 / 開店」の検索意図に対して市全体の情報を返せる構造へ変更。
- 地域ページにDB集計だけを使った説明文を追加。開店・閉店件数と直近12か月の動きを示し、推測記事は追加しない。
- 公開SEOページとサイトマップでは、住所がない低信頼データを除外。誤地域のニュース見出しが北海道ページ等へ混入する問題を抑制する。元データは削除しない。
- サイトマップに、十分な掲載件数がある地域・業種の `status=opening` / `status=closing` ページを追加。薄い空ページは大量生成しない。
- 札幌市の親ページを区データから生成できるようにし、市区町村の検索入口を増やす。

本番確認では、北海道ページが `北海道の開店閉店情報｜新店・閉店一覧`、北海道の閉店フィルタが `北海道の閉店情報｜閉店予定・閉店店舗一覧`、札幌市ページが `札幌市の開店閉店情報｜新店・閉店一覧`、札幌市の閉店フィルタが `札幌市の閉店情報｜閉店予定・閉店店舗一覧` としてHTTP 200で表示された。札幌市ページでは区を含め229件の掲載データを集約できた。旧北海道ページで見つかった名古屋・大阪・仙台等の明らかな地域不一致レコードは、新しい公開品質条件では表示されなくなった。

次の評価は「Bingで初めて表示回数が発生した検索語」と「着地ページ」を基準にする。表示が出始めた語に対してページを強化し、その後にチェーン別ページ（例: チェーン名＋新規オープン予定）や月別ページ（地域＋年月＋開店/閉店）を追加する。最初の表示が出る前に全国へ薄いページを無制限に増やさない。

## 実装判断の一次資料

- Google Search Central: https://developers.google.com/search/docs/crawling-indexing/links-crawlable
- Google Search Central: https://developers.google.com/search/docs/specialty/ecommerce/pagination-and-incremental-page-loading
- Google Search Central: https://developers.google.com/search/docs/essentials/spam-policies#scaled-content
- Google Search Central: https://developers.google.com/search/docs/crawling-indexing/ask-google-to-recrawl
