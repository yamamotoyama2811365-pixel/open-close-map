
.activity-wrap{display:grid;grid-template-columns:220px 1fr;gap:24px;align-items:stretch}
.activity-score{border-radius:16px;padding:24px;color:#fff;display:flex;flex-direction:column;justify-content:center;align-items:center;min-height:190px}
.activity-score span{font-size:10px;letter-spacing:.18em;font-weight:900;opacity:.85}
.activity-score strong{font-size:64px;line-height:1;margin-top:10px}
.activity-score small{font-size:13px;margin-top:5px;opacity:.8}
.score-a{background:linear-gradient(135deg,#0e6e4d,#26a269)}
.score-b{background:linear-gradient(135deg,#2d5c8e,#4c82b8)}
.score-c{background:linear-gradient(135deg,#9b6b18,#d3952c)}
.score-d{background:linear-gradient(135deg,#7a4141,#b65a5a)}
.activity-copy{border:1px solid var(--line);border-radius:16px;padding:22px}
.activity-copy h3{margin:0 0 8px;font-size:22px}
.activity-copy p{margin:0;color:var(--muted);line-height:1.8}
.activity-bars{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:18px}
.activity-bars div{background:#f5f8fa;border-radius:10px;padding:13px}
.activity-bars span{display:block;font-size:10px;color:var(--muted)}
.activity-bars b{display:block;font-size:22px;color:var(--navy);margin-top:4px}
.activity-note{font-size:11px;color:var(--muted);margin-top:12px}
@media(max-width:700px){.activity-wrap{grid-template-columns:1fr}.activity-bars{grid-template-columns:1fr}}
